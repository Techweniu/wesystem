// Em app/layout.tsx

import type React from "react"
import type { Metadata } from "next"
import "./globals.css"
import { Toaster } from "sonner"
import { ThemeProvider } from "@/components/theme-provider" // IMPORTADO AQUI

export const metadata: Metadata = {
  title: "BI Dashboard - Business Intelligence",
  description: "Dashboard de Business Intelligence para agências",
  generator: 'v0.app'

import { Poppins as V0_Font_Poppins } from 'next/font/google'

// Initialize fonts
const _poppins = V0_Font_Poppins({ subsets: ['latin'], weight: ["100","200","300","400","500","600","700","800","900"], variable: '--v0-font-poppins' })
const _v0_fontVariables = `${_poppins.variable}`

}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="pt-BR" suppressHydrationWarning>
      <body className={`antialiased ${_v0_fontVariables}`}>
        <ThemeProvider
          attribute="class"
          defaultTheme="dark"
          enableSystem
        >
          {children}
          <Toaster position="top-right" richColors />
        </ThemeProvider>
      </body>
    </html>
  )
}
